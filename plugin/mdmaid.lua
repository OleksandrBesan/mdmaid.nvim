-- mdmaid.nvim - Markdown + Mermaid preview for Neovim
-- This file is loaded automatically by Neovim

if vim.g.loaded_mdmaid then
  return
end
vim.g.loaded_mdmaid = true

-- Lazy-load the plugin when needed
-- Users should call require("mdmaid").setup() in their config
